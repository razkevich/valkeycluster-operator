---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: valkeyclusters.cache.razkevich.dev
spec:
  group: cache.razkevich.dev
  names:
    kind: ValkeyCluster
    listKind: ValkeyClusterList
    plural: valkeyclusters
    singular: valkeycluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.shards
      name: Shards
      type: integer
    - jsonPath: .spec.replicasPerShard
      name: Replicas
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.readyShards
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ValkeyCluster is the Schema for the valkeyclusters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ValkeyCluster
            properties:
              haPolicy:
                description: haPolicy tunes the clustering/HA settings and their performance
                  trade-offs.
                properties:
                  clusterNodeTimeoutMillis:
                    default: 5000
                    description: |-
                      clusterNodeTimeoutMillis is the failure-detection window in milliseconds
                      (maps to cluster-node-timeout). Trade-off: failover speed vs. false positives.
                    format: int32
                    minimum: 1000
                    type: integer
                  minReplicasToWrite:
                    default: 0
                    description: |-
                      minReplicasToWrite makes a primary refuse writes unless at least this many
                      replicas are connected and in sync (maps to min-replicas-to-write).
                      Trade-off: durability vs. write-availability. Default 0 (always accept writes).
                    format: int32
                    minimum: 0
                    type: integer
                  requireFullCoverage:
                    default: true
                    description: |-
                      requireFullCoverage controls whether the cluster serves its reachable slots
                      when some slots are unavailable (maps to cluster-require-full-coverage).
                      Trade-off: availability vs. correctness. Default true.
                    type: boolean
                type: object
              image:
                default: valkey/valkey:8
                description: image is the Valkey container image to run (must include
                  valkey-cli).
                type: string
              performance:
                description: performance tunes throughput/memory settings (io-threads,
                  eviction policy).
                properties:
                  ioThreads:
                    default: 1
                    description: |-
                      ioThreads sets the number of Valkey I/O threads for network read/write/parse
                      (command execution stays single-threaded). Higher values raise throughput on
                      multi-core nodes. Trade-off: CPU usage vs. throughput. (Valkey 8.)
                    format: int32
                    maximum: 128
                    minimum: 1
                    type: integer
                  maxmemoryPolicy:
                    default: noeviction
                    description: |-
                      maxmemoryPolicy is the behavior when maxmemory is reached. noeviction (default)
                      rejects writes — correct for a datastore; the allkeys-*/volatile-* policies evict —
                      correct for a cache (LFU beats LRU on skewed/hot-key access). Trade-off:
                      datastore correctness vs. cache graceful-degradation.
                    enum:
                    - noeviction
                    - allkeys-lru
                    - allkeys-lfu
                    - allkeys-random
                    - volatile-lru
                    - volatile-lfu
                    - volatile-random
                    - volatile-ttl
                    type: string
                type: object
              persistence:
                description: persistence configures durability (RDB vs. AOF) and its
                  trade-offs.
                properties:
                  appendFsync:
                    default: everysec
                    description: |-
                      appendFsync sets the AOF fsync cadence (used only when AOF is enabled):
                      always (safest, slowest), everysec (default, lose <=1s), or no (fastest).
                    enum:
                    - always
                    - everysec
                    - "no"
                    type: string
                  mode:
                    default: AOF
                    description: |-
                      mode selects the persistence mechanism(s): AOF (default), RDB, AOFAndRDB, or None.
                      Trade-off: AOF favors durability; RDB favors compactness and restart speed but
                      loses writes since the last snapshot; None is fastest (pure cache).
                    enum:
                    - AOF
                    - RDB
                    - AOFAndRDB
                    - None
                    type: string
                type: object
              replicasPerShard:
                default: 1
                description: replicasPerShard is the number of HA replica copies per
                  shard. 0 means no HA.
                format: int32
                maximum: 5
                minimum: 0
                type: integer
              resources:
                description: resources are the compute resources for the valkey container.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              shards:
                default: 3
                description: |-
                  shards is the number of data partitions (primaries). The keyspace is split
                  across shards. Must be 1 (HA-only, no sharding) or >= 3 (failover quorum).
                format: int32
                maximum: 16
                type: integer
                x-kubernetes-validations:
                - message: shards must be 1 or >= 3 (2 cannot form a failover quorum)
                  rule: self == 1 || self >= 3
              storage:
                description: storage configures the per-node persistent volume.
                properties:
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 1Gi
                    description: |-
                      size is the per-node persistent volume size. Immutable after creation
                      (volume expansion is out of scope).
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                    x-kubernetes-validations:
                    - message: storage.size is immutable
                      rule: self == oldSelf
                  storageClassName:
                    description: |-
                      storageClassName selects the StorageClass for the per-node PVCs.
                      Empty string uses the cluster default StorageClass.
                    type: string
                type: object
            type: object
          status:
            description: status defines the observed state of ValkeyCluster
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the ValkeyCluster resource
                  (Available, Progressing, Degraded).
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the .metadata.generation this status
                  reflects.
                format: int64
                type: integer
              phase:
                description: phase is the high-level lifecycle phase.
                type: string
              readyShards:
                description: readyShards is the number of shards with a reachable
                  primary and assigned slots.
                format: int32
                type: integer
              shards:
                description: shards holds the observed per-shard state.
                items:
                  description: ShardStatus is the observed state of a single shard.
                  properties:
                    index:
                      description: index is the shard ordinal.
                      format: int32
                      type: integer
                    nodeIDs:
                      description: nodeIDs are the Valkey node IDs that belong to
                        this shard.
                      items:
                        type: string
                      type: array
                    primaryNodeID:
                      description: primaryNodeID is the Valkey node ID of the current
                        primary.
                      type: string
                    primaryPod:
                      description: primaryPod is the pod currently acting as this
                        shard's primary.
                      type: string
                    readyReplicas:
                      description: readyReplicas is the number of in-sync replicas
                        for this shard.
                      format: int32
                      type: integer
                    slots:
                      description: slots is the hash-slot range owned by this shard
                        (e.g. "0-5460").
                      type: string
                  required:
                  - index
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - index
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
