# kind cluster for local RedisCluster operator development.
# Usage: kind create cluster --name rediscluster-dev --config hack/kind-cluster.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker
