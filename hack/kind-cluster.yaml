# kind cluster for local ValkeyCluster operator development.
# Usage: kind create cluster --name valkeycluster-dev --config hack/kind-cluster.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker
